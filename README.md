# Snake-io
Snake-IO is your terminal-based Snake game in Java, rocking a clean MVC setup: Snake model slithers around a grid, gobbling dual foods (apple + banana) to grow, while services handle input, collisions, spawning, scoring, and menu vibes
